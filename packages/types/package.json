{
  "name": "@kruxt/types",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "echo 'Add eslint config for shared types'",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "echo 'No type package tests yet'"
  },
  "devDependencies": {
    "typescript": "^5.8.2"
  }
}
